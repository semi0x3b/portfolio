# DESIGN.md — semi0x3b.github.io/portfolio

백엔드·인프라 엔지니어 개인 포트폴리오. 단일 `index.html`, 프레임워크 없음, CSS 변수가 곧 토큰이다.
UI를 고치기 전에 이 파일을 읽고, 여기 없는 색·서체·라운드를 새로 만들지 않는다.

## 인상

청록이 섞인 먹색 위에 앰버 하나. "흔한 다크 포트폴리오"(순검정 + 형광 단색, 보라 그라데이션)와 구분되는 지점이 배경의 녹색 기운이다.
꾸밈보다 실측 수치와 실제 산출물(다이어그램·시연 영상·평가 원문)이 페이지를 끌고 간다. 장식은 없다.
골격은 2026-09-18에 getdesign.md 샘플 12종을 비교한 뒤 Sentry(헤드라인 500 대 수치 700의 굵기 대비)와 Supabase(6px 버튼, 4px 칩, 낮은 굵기 제목)를 합쳐 정했다. 색은 바꾸지 않았다.

## 색

| 토큰 | 값 | 역할 |
|---|---|---|
| `--bg` | `#0B1411` | 페이지 배경. 순검정 금지 |
| `--card` | `#13211C` | 프로젝트 카드, 버튼 바탕 |
| `--card-2` | `#182A23` | 이미지 로딩 전 바탕 |
| `--line` | `#23382F` | 모든 테두리와 구분선. 1px |
| `--text` | `#E7EDE8` | 본문 |
| `--muted` | `#94A69B` | 보조 텍스트, 날짜, 출처 |
| `--accent` | `#E0A03A` | 수치 강조, 주 버튼, 로고, 포커스 링. 페이지에 하나뿐인 강조색 |
| `--mint` | `#79B394` | 기술 칩과 링크. 강조가 아니라 분류용 |

주 버튼 글자는 `--bg`(대비 10:1). 흰 글자는 앰버 위에서 2.3:1이라 쓰지 않는다.
그라데이션, 블러 글로우, 그림자 없음.

## 서체

Pretendard Variable 하나(jsdelivr CDN). 로고 `semi;`만 시스템 모노스페이스.
JetBrains Mono 같은 모노 폰트를 날짜·라벨에 쓰지 않는다.

| 용도 | 크기 | 굵기 | 자간 |
|---|---|---|---|
| 히어로 문장(h1, 줄마다 `span`) | `clamp(1.5rem, 3.2vw, 2.5rem)` / 1.4 | 500, 수치 `b`는 700 앰버 | -.015em |
| 섹션 제목(h2) | `clamp(1.6rem, 3vw, 2.1rem)` | 500 | -.015em |
| 큰 인용 | `clamp(1.2rem, 2.2vw, 1.55rem)` / 1.6 | 400 | -.01em |
| 본문 | 17px / 1.75 | 400 | 0 |
| 카드 본문·불릿·인용 | 15.5px / 1.7 | 400 | 0 |
| 보조·출처·날짜 | 14~14.5px | 400 | 0 |

`word-break: keep-all` 필수. 히어로 h1은 `text-wrap: balance`.
`-webkit-font-smoothing: antialiased`를 쓰지 않는다. 다크 배경에서 500 굵기 획이 얇아져 "깨져 보인다"는 지적을 받고 뺐다(2026-09-18). 13px 이하 글자도 쓰지 않는다.
제목(h2·h3·회사명·글 제목)은 전부 500이고, 700은 수치 강조(`b`, `.metric em`)에만 쓴다. 이 대비가 페이지의 위계다. 600·750·800은 쓰지 않는다.

## 간격·폭

- 컨테이너 1180px, 좌우 `clamp(20px, 5vw, 56px)`.
- 섹션 사이 `clamp(64px, 9vw, 112px)`. 섹션 제목 아래 `clamp(28px, 4vw, 44px)`.
- 본문 `max-width` 제한 없음. 사용자가 "가로 너비 전체를 써야 한다"고 결정(2026-09-17).
- 라운드: 카드 8px, 버튼·더보기 6px, 칩 4px, 프로필 사진 원형(50%). 알약(999px)은 사진 외에 쓰지 않는다. 그 외 값 만들지 않는다.

## 컴포넌트

- **히어로**: 원형 사진 88px + 한 줄 소개(`--muted`), 실측 수치 세 문장, 버튼 둘(주: 프로젝트 보기, 보조: 연락하기). 다이어그램·아바타·뱃지 없음.
- **프로젝트 카드**: 이 페이지에서 카드 모양을 쓰는 유일한 곳. 16:10 썸네일(클릭하면 `dialog` 확대) → 기간(한글, "2026년 5월 – 7월") → h3 → 설명 → 기술 칩 → 링크(밑줄, `--mint`) → 하단 구분선 위 핵심 수치(`em`은 앰버). hover는 테두리색만 바뀐다. 떠오르는 효과 없음.
- **경력**: 카드 없음. `170px 1fr` 격자, 왼쪽 기간, 오른쪽 회사명(h3)·역할(앰버)·설명·불릿. 항목 사이 1px 구분선.
- **동료 평가**: 큰 인용 1건 + 3열 격자 3건, 나머지 8건은 `<details>` "평가 8건 더 보기". 출처는 `<cite>` "회사 역할, 출처" 형식으로 이름 없이. 이니셜 원, 별점, 카드 없음.
- **글·코드**: 두 열 목록, 항목 사이 구분선. 패널 배경 없음.
- **연락**: 상단 구분선, 주 버튼에 이메일 주소 그대로.

## 하지 않는 것

이 목록은 실제로 넣었다가 뺀 것들이다. 다시 넣지 않는다.

- 영문 eyebrow 라벨("About", "Projects"), 대문자 자간 벌린 라벨
- 중점으로 이은 메타("2026.05 – 07 · Infra"), 링크 끝 "↗", "라벨 — 설명" 형식 불릿
- 스킬 카드 6개, 큰 숫자 stat band, 미니스탯, 기술 스택 칩 패널, 이모지 아이콘
- 스크롤 진입 페이드인, hover translateY, 배경 글로우
- 히어로에 다이어그램 전폭 배치(뜬금없다는 지적)
- 인사말 히어로("안녕하세요, ○○○입니다")

## 내용 규칙

수치는 `~/notes/content/career/ledger.md` 원문을 복사한다. 원장에 없는 합계를 만들지 않고, "최대·평균" 한정어를 떼지 않는다.
사내 기준 수치(문서 90+, 프로젝트 59건), 근거 없는 수치(CloudFormation 스택 16개)는 쓰지 않는다.
평가 인용은 강점 문장만, 이름 없이. 팀 구성은 "백엔드 1(본인), 프론트 2" 식으로 밝히고 "단독"이라 쓰지 않는다.

## 접근성·동작

- `:focus-visible` 앰버 2px 링. `prefers-reduced-motion`이면 transition 전부 끔.
- 모바일(≤640px) 네비를 숨기지 않고 줄바꿈. 가로 스크롤 0.
- 변경 후 확인: Playwright 헤드리스로 1440·390px 풀페이지 스크린샷, 콘솔 에러 0, 4xx 응답 0.
- og.png는 사진 원형 300px + 히어로 첫 두 문장, 1200×630. 히어로 문구가 바뀌면 같이 다시 만든다.
